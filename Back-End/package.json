{
  "name": "history-around-backend",
  "version": "1.0.0",
  "description": "Express.js server for History Around React app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo \"Building React app...\" && cd ../Front-End/history-around-web && npm run build && cd ../../Back-End && npm run copy-build",
    "copy-build": "node copy-build.js",
    "deploy": "npm install && cd ../Front-End/history-around-web && npm install && npm run build && cd ../../Back-End && npm run copy-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["express", "react", "history", "web-app"],
  "author": "tzanislav",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
